import 'package:flutter/material.dart';
import 'Setupdetailes.dart';

class Setuppage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      title: 'Setup Up Page',
      home: Setupdetailes(),
    );
  }
}
